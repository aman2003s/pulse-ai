models/
__pycache__/
venv/
*.sqlite3
*.db

pulse_demo.mp4
ui/pulse-ui.exe
.claude/
pulse.egg-info/
ui/node_modules/
ui/src-tauri/target/
scratch_demo*.py

# PyInstaller build output for the packaged installer's backend (see
# docs/INSTALLER_PLAN.md) — built locally via the command in that doc,
# never committed (the frozen core alone is >1GB).
/build/
/dist/
